export function repeat(nn, xx) {
  return xx.repeat(nn)
}

export default repeat
export const FUNCTION = repeat
export const ARITY = 2
export const SIGNATURE = ["number", "Object|string", "Object|string"]
