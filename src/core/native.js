export const isArray = Array.isArray
export const keys = Object.keys
export const values = Object.values
export const freeze = Object.freeze
export const round = Math.round
export function trim(xx) {
  return xx.trim()
}
